@page "/Account/Manage/GenerateRecoveryCodes"

@inject IdentityRedirectManager RedirectManager

<PageTitle>Presmerovanie - ZISK</PageTitle>

@code {
    protected override void OnInitialized()
        => RedirectManager.RedirectTo("/profil");
}

@*
@using Microsoft.AspNetCore.Identity
@using ZISK.Data

@inject UserManager<ApplicationUser> UserManager
@inject IdentityUserAccessor UserAccessor
@inject ILogger<GenerateRecoveryCodes> Logger

<PageTitle>Vygenerovať záložné kódy pre 2FA</PageTitle>

@if (recoveryCodes is not null)
{
    <ShowRecoveryCodes RecoveryCodes="recoveryCodes.ToArray()" StatusMessage="@message" />
}
else
{
    <h3>Vygenerovať záložné kódy pre 2FA</h3>
    <div class="alert alert-warning" role="alert">
        <p>
            <span class="glyphicon glyphicon-warning-sign"></span>
            <strong>Uložte si tieto kódy na bezpečné miesto.</strong>
        </p>
        <p>
            Ak stratíte svoje zariadenie a nemáte záložné kódy, stratíte prístup k svojmu účtu.
        </p>
        <p>
            Generovanie nových záložných kódov nezmení kľúče používané v aplikáciách autentifikátora. Ak chcete zmeniť kľúč
            používaný v aplikácii autentifikátora, mali by ste <a href="Account/Manage/ResetAuthenticator">resetovať svoje autentifikačné kľúče.</a>
        </p>
    </div>
    <div>
        <form @formname="generate-recovery-codes" @onsubmit="OnSubmitAsync" method="post">
            <AntiforgeryToken />
            <button class="btn btn-danger" type="submit">Vygenerovať záložné kódy</button>
        </form>
    </div>
}

@code {
    private string? message;
    private ApplicationUser user = default!;
    private IEnumerable<string>? recoveryCodes;

    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    protected override async Task OnInitializedAsync()
    {
        user = await UserAccessor.GetRequiredUserAsync(HttpContext);

        var isTwoFactorEnabled = await UserManager.GetTwoFactorEnabledAsync(user);
        if (!isTwoFactorEnabled)
        {
            throw new InvalidOperationException("Cannot generate recovery codes for user because they do not have 2FA enabled.");
        }
    }

    private async Task OnSubmitAsync()
    {
        var userId = await UserManager.GetUserIdAsync(user);
        recoveryCodes = await UserManager.GenerateNewTwoFactorRecoveryCodesAsync(user, 10);
        message = "Vygenerovali ste nové záložné kódy.";

        Logger.LogInformation("User with ID '{UserId}' has generated new 2FA recovery codes.", userId);
    }
}
*@
