@inherits ZISK.Client.Components.LocalizedComponentBase
@using ZISK.Client.Components
@using ZISK.Client.Services
@*
    "Moje deti" (parents only): pick a child and view their read-only details.
*@

<ProfileSectionHeading Icon="@Icons.Material.Rounded.FamilyRestroom" Title="@T("profile.nav.children")" />

@if (IsLoading)
{
    <MudProgressLinear Indeterminate="true" Color="Color.Primary" />
}
else if (Children.Count == 0)
{
    <MudAlert Severity="Severity.Info" Variant="Variant.Outlined">
        @T("profile.noChildrenAssigned")
    </MudAlert>
}
else
{
    @if (Children.Count > 1)
    {
        <MudSelect T="string" Value="SelectedChildId" ValueChanged="OnChildSelected"
                   Label="@T("profile.selectChild")" Variant="Variant.Outlined"
                   Class="mb-4" Style="max-width:360px;">
            @foreach (var child in Children)
            {
                <MudSelectItem @key="child.Id" T="string" Value="@child.Id">@child.FirstName @child.LastName</MudSelectItem>
            }
        </MudSelect>
    }

    @if (SelectedChild is not null)
    {
        <MudGrid Spacing="3">
            <MudItem xs="12" sm="6">
                <ReadOnlyField Label="@T("admin.users.table.firstName")" Value="@SelectedChild.FirstName" />
            </MudItem>
            <MudItem xs="12" sm="6">
                <ReadOnlyField Label="@T("admin.users.table.lastName")" Value="@SelectedChild.LastName" />
            </MudItem>
            <MudItem xs="12" sm="6">
                <ReadOnlyField Label="@T("dashboard.child.dateOfBirth")" Value="@SelectedChildBirthDateText" />
            </MudItem>
            <MudItem xs="12" sm="6">
                <ReadOnlyField Label="@T("dashboard.team")" Value="@(SelectedChild.TeamName ?? "—")" />
            </MudItem>
        </MudGrid>
    }
}

@code {
    [Parameter] public bool IsLoading { get; set; }
    [Parameter, EditorRequired] public IReadOnlyList<ChildDto> Children { get; set; } = [];

    [Parameter] public string? SelectedChildId { get; set; }
    [Parameter] public EventCallback<string> OnChildSelected { get; set; }

    [Parameter] public ChildDto? SelectedChild { get; set; }
    [Parameter] public string SelectedChildBirthDateText { get; set; } = "—";
}
