@page "/ospravedlnenky/vytvorit"
@inject NavigationManager NavigationManager
@attribute [Authorize]

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/ospravedlnenky?create=true", replace: true);
    }
}
