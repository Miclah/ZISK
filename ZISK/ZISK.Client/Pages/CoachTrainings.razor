@page "/coach/treningy"
@inject NavigationManager NavigationManager
@attribute [Authorize(Roles = "Coach")]

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/kalendar", replace: true);
    }
}
